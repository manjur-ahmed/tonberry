name: Deploy tonberry web

on:
  push:
    branches: [main]
    paths:
      - "web/**"
      - ".github/workflows/deploy-web.yml"
  workflow_dispatch: {}

permissions:
  id-token: write # required for OIDC — no stored AWS keys
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: web/package-lock.json

      - run: npm ci

      - run: npm run build
        env:
          VITE_API_URL: ${{ vars.TONBERRY_API_URL }}

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars.TONBERRY_GITHUB_DEPLOY_ROLE_ARN }}
          aws-region: ${{ vars.TONBERRY_AWS_REGION }}

      - run: aws s3 sync dist/ s3://${{ vars.TONBERRY_S3_BUCKET }} --delete

      - run: |
          aws cloudfront create-invalidation \
            --distribution-id ${{ vars.TONBERRY_CLOUDFRONT_DISTRIBUTION_ID }} \
            --paths "/*"
